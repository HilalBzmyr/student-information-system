# Installation Guide

## Requirements
- Ubuntu 20.04 or newer
- Docker
- Docker Compose
- Git

## Setup Steps
1. Clone the project repository
2. Navigate to the project directory
3. Run docker-compose up
4. Use the application via command line interface

## Notes
- The database runs in a separate Docker container
- Configuration is handled via Docker Compose
